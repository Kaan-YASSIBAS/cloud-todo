name: Release Please (per-service)

on:
  push:
    branches: ["kaan-branch"]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          manifest: true
          target-branch: kaan-branch
