apiVersion: v1
kind: ConfigMap
metadata:
  name: task-config
  namespace: cloud-todo
data:
  DB_NAME: tododb
  # Optional CORS for frontend (comma-separated). Leave empty or remove in prod.
  CORS_ALLOW_ORIGINS: "http://localhost:8000,http://127.0.0.1:8000"